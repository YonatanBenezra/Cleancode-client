import React from "react";

const Dashboard = () => {
  return (
    <div>
      <h2>
        Design a beautiful table with a (column: Id, User Name, Exercise Name,
        Description, Actions (Edit Icon, Delete, Approve) )
      </h2>
    </div>
  );
};

export default Dashboard;
